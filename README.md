# Set up on a new machine

```
curl https://raw.githubusercontent.com/graysonwright/dotfiles/master/laptop.local > ~/.laptop.local && bash <(curl -s https://raw.githubusercontent.com/thoughtbot/laptop/master/mac) 2>&1 | tee ~/laptop.log
```
